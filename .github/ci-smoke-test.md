# CI smoke test

Arquivo temporário para validar a **branch protection** e os **required checks**
em um Pull Request. É seguro deletar — feche este PR sem fazer merge assim que
os checks estiverem verdes.
